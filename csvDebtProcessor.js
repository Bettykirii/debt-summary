//function that summarizes the data from the csv file

function summarizeCSV(data) {
    //create an object for the debts

    const summary = {};

    data.forEach(row => {

        data.forEach(row => {
            // Access elements using indexing
            const debtor = row[0];     // First element
            const creditor = row[1];   // Second element
            const amount = row[2];      // Third element
        
        });

        //convert the number into a float to accomodate  decimals

    const amountNum = parseFloat(amount);


        
        
    });
}